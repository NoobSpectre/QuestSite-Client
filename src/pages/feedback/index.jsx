import { Button, Container } from '@mui/material';

const Feedback = () => {
  return (
    <Container maxWidth={'xs'}>
      <Button>Welcome to Feedback page</Button>
    </Container>
  );
};

export default Feedback;
